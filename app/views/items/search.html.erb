<h1>Search Results</h1>

<% if @items.any? %>
  <ul>
    <% @items.each do |item| %>
      <li><%= link_to item.item_name, item_path(item) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No items found</p>
<% end %>
