<h2>Your Cart</h2>

<% if @cart && @cart.items.any? %>
  <ul>
    <% @cart.items.each do |item| %>
      <li>
        <strong><%= item.item_name %></strong> - <%= number_to_currency(item.price) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Your cart is empty.</p>
<% end %>
